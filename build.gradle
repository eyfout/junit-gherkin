allprojects {
    group "ht.eyfout.junit"
    version "0.0.1"
}
subprojects {
    repositories {
        mavenCentral()
        mavenLocal()
    }
    plugins.apply("maven-publish")
    publishing {
        repositories {
            mavenLocal()
        }
    }
}