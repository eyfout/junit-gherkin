allprojects {
    group "ht.eyfout"
    version "0.0.1"
}
subprojects {
    repositories {
        mavenLocal()
        mavenCentral()
    }
    plugins.apply("maven-publish")
    publishing {
        repositories {
            mavenLocal()
        }
    }
}