subprojects {
    repositories {
        mavenLocal()
        mavenCentral()
    }
    plugins.apply("maven-publish")
    publishing {
        repositories {
            mavenLocal()
        }
    }
}